{
    "builds": [
      {
        "src": "Authentication/wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "Authentication/wsgi.py"
      }
    ]
}